import { nonImplemente } from '@/lib/api-stub';

// PATCH + DELETE /api/projets/[id]
export async function PATCH() {
  return nonImplemente('①');
}

export async function DELETE() {
  return nonImplemente('①');
}
