// Backlog — filtres, tri, sections, swipe (tranche ①)
export default function BacklogPage() {
  return (
    <main>
      <h1>Backlog</h1>
      <p>Tranche ① — liste des tâches à implémenter.</p>
    </main>
  );
}
